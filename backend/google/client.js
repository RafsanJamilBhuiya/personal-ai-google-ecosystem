import { getValidAccessToken } from "./token-store.js";
const BASE="https://www.googleapis.com";
export async function googleFetch(env,path,options={}){const token=await getValidAccessToken(env);const headers=new Headers(options.headers||{});headers.set("authorization",`Bearer ${token}`);headers.set("accept","application/json");let response=await fetch(path.startsWith("http")?path:`${BASE}${path}`,{...options,headers});if(response.status===401){const fresh=await getValidAccessToken(env);headers.set("authorization",`Bearer ${fresh}`);response=await fetch(path.startsWith("http")?path:`${BASE}${path}`,{...options,headers});}if(!response.ok){const text=await response.text();const e=new Error(`Google API request failed (${response.status})`);e.code="GOOGLE_API_ERROR";e.status=response.status;e.details=text.slice(0,500);throw e;}if(response.status===204)return null;const text=await response.text();try{return text?JSON.parse(text):null;}catch{return {raw:text};}}
